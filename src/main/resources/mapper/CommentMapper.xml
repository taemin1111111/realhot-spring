<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wherehot.spring.mapper.CommentMapper">

    <!-- 결과 매핑 -->
    <resultMap id="CommentResultMap" type="com.wherehot.spring.entity.Comment">
        <id property="id" column="id"/>
        <result property="postId" column="post_id"/>
        <result property="userid" column="userid"/>
        <result property="userip" column="userip"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="nickname" column="nickname"/>
        <result property="passwd" column="passwd"/>
        <result property="likes" column="likes"/>
        <result property="dislikes" column="dislikes"/>
        <result property="isDeleted" column="is_deleted"/>
    </resultMap>

    <!-- 게시글별 댓글 조회 -->
    <select id="getCommentsByPostId" resultMap="CommentResultMap">
        SELECT id, post_id, userid, userip, content, created_at, nickname, passwd, likes, dislikes, is_deleted
        FROM hottalk_comment 
        WHERE post_id = #{postId} AND is_deleted = 0
        ORDER BY created_at ASC
    </select>

    <!-- 댓글 등록 -->
    <insert id="insertComment" parameterType="com.wherehot.spring.entity.Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO hottalk_comment (
            post_id, userid, userip, content, created_at, nickname, passwd, likes, dislikes, is_deleted
        ) VALUES (
            #{postId}, #{userid}, #{userip}, #{content}, NOW(), #{nickname}, #{passwd}, #{likes}, #{dislikes}, #{isDeleted}
        )
    </insert>

    <!-- 댓글 수정 -->
    <update id="updateComment" parameterType="com.wherehot.spring.entity.Comment">
        UPDATE hottalk_comment 
        SET 
            content = #{content},
            nickname = #{nickname},
            passwd = #{passwd}
        WHERE id = #{id}
    </update>

    <!-- 댓글 삭제 -->
    <update id="deleteComment">
        UPDATE hottalk_comment 
        SET is_deleted = 1
        WHERE id = #{id}
    </update>

    <!-- 댓글 수 조회 -->
    <select id="getCommentCountByPostId" resultType="int">
        SELECT COUNT(*)
        FROM hottalk_comment 
        WHERE post_id = #{postId} AND is_deleted = 0
    </select>

</mapper>
