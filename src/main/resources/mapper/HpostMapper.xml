<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wherehot.spring.mapper.HpostMapper">

    <!-- 결과 매핑 -->
    <resultMap id="HpostResultMap" type="com.wherehot.spring.entity.Hpost">
        <id property="id" column="id"/>
        <result property="userid" column="userid"/>
        <result property="userip" column="userip"/>
        <result property="nickname" column="nickname"/>
        <result property="passwd" column="passwd"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="views" column="views"/>
        <result property="photo1" column="photo1"/>
        <result property="photo2" column="photo2"/>
        <result property="photo3" column="photo3"/>
        <result property="likes" column="likes"/>
        <result property="dislikes" column="dislikes"/>
        <result property="reports" column="reports"/>
        <result property="createdAt" column="created_at"/>
        <result property="commentCount" column="comment_count"/>
        <result property="level" column="level"/>
    </resultMap>

    <!-- 최신순 게시글 목록 조회 -->
    <select id="selectLatestHpostList" resultMap="HpostResultMap">
        SELECT 
            h.id, h.userid, h.userip, h.nickname, h.passwd, h.title, h.content, 
            h.views, h.photo1, h.photo2, h.photo3, h.likes, h.dislikes, h.reports, h.created_at,
            COALESCE(c.comment_count, 0) as comment_count,
            m.level_id as level
        FROM hottalk_post h
        LEFT JOIN (
            SELECT post_id, COUNT(*) as comment_count 
            FROM hottalk_comment 
            GROUP BY post_id
        ) c ON h.id = c.post_id
        LEFT JOIN member m ON h.userid = m.userid
        ORDER BY h.created_at DESC 
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 인기순 정렬 (조회수 20%, 좋아요 50%, 댓글 30%) -->
    <select id="selectPopularHpostList" resultMap="HpostResultMap">
        SELECT 
            h.id, h.userid, h.userip, h.nickname, h.passwd, h.title, h.content, 
            h.views, h.photo1, h.photo2, h.photo3, h.likes, h.dislikes, h.reports, h.created_at,
            COALESCE(c.comment_count, 0) as comment_count,
            m.level_id as level
        FROM hottalk_post h
        LEFT JOIN (
            SELECT post_id, COUNT(*) as comment_count 
            FROM hottalk_comment 
            GROUP BY post_id
        ) c ON h.id = c.post_id
        LEFT JOIN member m ON h.userid = m.userid
        ORDER BY (h.views * 0.2 + h.likes * 0.5 + COALESCE(c.comment_count, 0) * 0.3) DESC, h.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 전체 게시글 수 조회 -->
    <select id="selectTotalHpostCount" resultType="int">
        SELECT COUNT(*) FROM hottalk_post
    </select>
    
    <!-- 검색 게시글 목록 조회 (최신순) -->
    <select id="searchLatestHpostList" resultMap="HpostResultMap">
        SELECT 
            h.id, h.userid, h.userip, h.nickname, h.passwd, h.title, h.content, 
            h.views, h.photo1, h.photo2, h.photo3, h.likes, h.dislikes, h.reports, h.created_at,
            COALESCE(c.comment_count, 0) as comment_count,
            m.level_id as level
        FROM hottalk_post h
        LEFT JOIN (
            SELECT post_id, COUNT(*) as comment_count 
            FROM hottalk_comment 
            GROUP BY post_id
        ) c ON h.id = c.post_id
        LEFT JOIN member m ON h.userid = m.userid
        <where>
            <if test="searchType == 'title'">
                AND h.title LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
            <if test="searchType == 'content'">
                AND h.content LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
            <if test="searchType == 'all'">
                AND (h.title LIKE CONCAT('%', #{searchKeyword}, '%') 
                     OR h.content LIKE CONCAT('%', #{searchKeyword}, '%'))
            </if>
        </where>
        ORDER BY h.created_at DESC 
        LIMIT #{offset}, #{limit}
    </select>
    
    <!-- 검색 게시글 목록 조회 (인기순) -->
    <select id="searchPopularHpostList" resultMap="HpostResultMap">
        SELECT 
            h.id, h.userid, h.userip, h.nickname, h.passwd, h.title, h.content, 
            h.views, h.photo1, h.photo2, h.photo3, h.likes, h.dislikes, h.reports, h.created_at,
            COALESCE(c.comment_count, 0) as comment_count,
            m.level_id as level
        FROM hottalk_post h
        LEFT JOIN (
            SELECT post_id, COUNT(*) as comment_count 
            FROM hottalk_comment 
            GROUP BY post_id
        ) c ON h.id = c.post_id
        LEFT JOIN member m ON h.userid = m.userid
        <where>
            <if test="searchType == 'title'">
                AND h.title LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
            <if test="searchType == 'content'">
                AND h.content LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
            <if test="searchType == 'all'">
                AND (h.title LIKE CONCAT('%', #{searchKeyword}, '%') 
                     OR h.content LIKE CONCAT('%', #{searchKeyword}, '%'))
            </if>
        </where>
        ORDER BY (h.views * 0.2 + h.likes * 0.5 + COALESCE(c.comment_count, 0) * 0.3) DESC, h.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <!-- 검색 게시글 수 조회 -->
    <select id="selectSearchHpostCount" resultType="int">
        SELECT COUNT(*) FROM hottalk_post h
        <where>
            <if test="searchType == 'title'">
                AND h.title LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
            <if test="searchType == 'content'">
                AND h.content LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
            <if test="searchType == 'all'">
                AND (h.title LIKE CONCAT('%', #{searchKeyword}, '%') 
                     OR h.content LIKE CONCAT('%', #{searchKeyword}, '%'))
            </if>
        </where>
    </select>

    <!-- 게시글 상세 조회 -->
    <select id="selectHpostById" resultMap="HpostResultMap">
        SELECT 
            id, userid, userip, nickname, passwd, title, content, 
            views, photo1, photo2, photo3, likes, dislikes, reports, created_at
        FROM hottalk_post 
        WHERE id = #{id}
    </select>

    <!-- 조회수 증가 -->
    <update id="incrementViewCount">
        UPDATE hottalk_post 
        SET views = views + 1 
        WHERE id = #{id}
    </update>
    
    <!-- 게시글 저장 -->
    <insert id="insertHpost" parameterType="com.wherehot.spring.entity.Hpost" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO hottalk_post (
            userid, userip, nickname, passwd, title, content, 
            views, photo1, photo2, photo3, likes, dislikes, reports, created_at
        ) VALUES (
            #{userid}, #{userip}, #{nickname}, #{passwd}, #{title}, #{content},
            #{views}, #{photo1}, #{photo2}, #{photo3}, #{likes}, #{dislikes}, #{reports}, #{createdAt}
        )
    </insert>
    
    <!-- 좋아요/싫어요 수 업데이트 -->
    <update id="updateVoteCounts">
        UPDATE hottalk_post 
        SET likes = #{likes}, dislikes = #{dislikes}
        WHERE id = #{postId}
    </update>
    
    <!-- 게시글 삭제 -->
    <delete id="deleteHpost">
        DELETE FROM hottalk_post WHERE id = #{id}
    </delete>
    
</mapper>
